<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Seat Reservation App</title>
    <link rel="stylesheet" href="/css/stylesheet.css">
	<script src="/js/send_url.js"></script>
</head>
<body>
    <%- include('header'); %>
    <h1>座席表</h1>
    <h3>座席を選択してください。</h3>
    <div id="seat-container">
        <% seats.forEach((seat) => { %>
            <% if(seat.user_id === 0){ %>
                <div class="seat available" data-seat-number="<%=seat.seat_number%>"><%=seat.seat_number%></div>
            <% } else { %>
                <div class="seat reserved" data-seat-number="<%=seat.seat_number%>"><%=seat.seat_number%></div>
            <% } %>
        <% }) %>
    </div>
    <div id="study-container" style="display: none;">
        <h2>Study Session</h2>
        <label for="study-content">Study Content:</label>
        <input type="text" id="study-content" />
        <div id="stopwatch">
            <span id="time">00:00:00</span>
            <button id="start">Start</button>
            <button id="stop" disabled>Stop</button>
        </div>
    </div>
    <script src="/js/room_script.js"></script>
</body>
</html>
